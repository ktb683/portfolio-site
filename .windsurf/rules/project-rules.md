---
trigger: always_on
---

We use `pnpm` instead of `npm`.

Don't use overly defensive code.

Run `pnpm lint` after you finish every task.

Always use idiomatic tailwind when handling styles and styling.

This is a static site for a personal portfolio.